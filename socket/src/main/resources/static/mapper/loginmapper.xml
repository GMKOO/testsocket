<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.socket.LoginDAO">



	<select id="login"  parameterType="Map" resultType="Map">
	
	SELECT m_name, count(*) AS 'count' FROM testjoin
	 WHERE m_id= #{id} AND m_pw= #{pw}
	

	</select>
	
	<select id="checkID"  parameterType="Map" resultType="Map">
	
	SELECT m_name, count(*) AS 'count' FROM testjoin
	 WHERE m_id= #{id} AND m_pw= #{pw}
	

	</select>
	

	
	<select id="joincheckID" parameterType="String" resultType="Integer">
	
	SELECT count(*) FROM testjoin WHERE m_id= #{id}
	
	
	</select>
	


	<insert id="joinCreateID" parameterType="Map">
	

    INSERT INTO testjoin (m_id, m_pw, m_name)
    VALUES (#{id}, #{pw}, #{name} )
    
</insert>

	


	</mapper>